---
interface Props {
	date?: Date;
}

const { date } = Astro.props;

if (!date) {
	throw new Error('FormattedDate requires a date prop');
}
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("es-ES", {
			year: "numeric",
			month: "long",
			day: "numeric",
		})
	}
</time>
